{
  try {
    this.loader=new URLClassLoader(new URL[]{new File(LiquibasePreferences.getCurrentChangeLogFileName()).getParentFile().toURL()});
  }
 catch (  MalformedURLException e) {
    throw new RuntimeException(e);
  }
}
