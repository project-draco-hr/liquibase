{
  String catalogName=getCatalogName();
  String schemaName=getName();
  if (schemaName == null) {
    schemaName="DEFAULT";
  }
  if (catalogName == null) {
    return schemaName;
  }
 else {
    return catalogName + "." + schemaName;
  }
}
