{
  String sql=getSql();
  if (sql == null) {
    sql="";
  }
  return CheckSum.compute(this.getEndDelimiter() + ":" + this.isSplitStatements()+ ":"+ this.isStripComments()+ ":"+ prepareSqlForChecksum(sql));
}
