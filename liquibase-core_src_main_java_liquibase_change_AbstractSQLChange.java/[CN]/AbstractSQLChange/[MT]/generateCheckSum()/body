{
  String sql=getSql();
  if (sql == null) {
    sql="";
  }
  return CheckSum.compute(this.getEndDelimiter() + ":" + this.isSplittingStatements()+ ":"+ this.isStrippingComments()+ ":"+ normalizeLineEndings(sql));
}
