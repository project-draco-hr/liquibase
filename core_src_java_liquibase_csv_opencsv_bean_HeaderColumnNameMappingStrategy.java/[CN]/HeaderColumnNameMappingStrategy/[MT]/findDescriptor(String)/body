{
  if (null == descriptors)   descriptors=loadDescriptors(getType());
  for (int i=0; i < descriptors.length; i++) {
    PropertyDescriptor desc=descriptors[i];
    if (matches(name,desc))     return desc;
  }
  return null;
}
