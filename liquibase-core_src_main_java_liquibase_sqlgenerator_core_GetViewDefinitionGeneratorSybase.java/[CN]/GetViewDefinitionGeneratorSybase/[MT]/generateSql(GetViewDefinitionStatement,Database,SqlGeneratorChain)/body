{
  String sql="select text from syscomments where id = object_id('" + database.correctSchemaName(statement.getSchemaName()) + "."+ statement.getViewName()+ "') order by colid";
  return new Sql[]{new UnparsedSql(sql)};
}
