{
  try {
    String sql="select text from syscomments where id = object_id('" + database.convertRequestedSchemaToSchema(statement.getSchemaName()) + "."+ statement.getViewName()+ "') order by colid";
    return new Sql[]{new UnparsedSql(sql)};
  }
 catch (  DatabaseException e) {
    throw new UnexpectedLiquibaseException(e);
  }
}
