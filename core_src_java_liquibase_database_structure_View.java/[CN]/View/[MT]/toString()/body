{
  String viewStr=getName() + " (";
  for (int i=0; i < columns.size(); i++) {
    if (i > 0) {
      viewStr+="," + columns.get(i);
    }
 else {
      viewStr+=columns.get(i);
    }
  }
  viewStr+=")";
  return viewStr;
}
