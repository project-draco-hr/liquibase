{
  String tableStr=getName() + " (";
  for (int i=0; i < columns.size(); i++) {
    if (i > 0) {
      tableStr+="," + columns.get(i).getName();
    }
 else {
      tableStr+=columns.get(i).getName();
    }
  }
  tableStr+=")";
  return tableStr;
}
