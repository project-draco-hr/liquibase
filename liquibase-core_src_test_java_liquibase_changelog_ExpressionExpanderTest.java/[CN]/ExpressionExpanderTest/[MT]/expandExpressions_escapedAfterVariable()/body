{
  this.handler=new ChangeLogParameters.ExpressionExpander(changeLogParameters,true);
  assertEquals(System.getProperty("user.name") + " is a variable, ${user.name} is a literal",handler.expandExpressions("${user.name} is a variable, ${:user.name} is a literal"));
}
