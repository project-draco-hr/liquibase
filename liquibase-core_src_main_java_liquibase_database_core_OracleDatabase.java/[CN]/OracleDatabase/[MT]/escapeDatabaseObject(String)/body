{
  if (objectName != null && (Pattern.compile("\\W").matcher(objectName).find() || reservedWords.contains(objectName.toUpperCase()))) {
    return "\"" + objectName + "\"";
  }
 else {
    return objectName;
  }
}
