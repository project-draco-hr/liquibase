{
  for (  Database database : TestContext.getInstance().getAllDatabases()) {
    if (database instanceof DB2Database || database instanceof DB2Database) {
      super.testAddAutoIncrementStatement(database,BigInteger.valueOf(2),null,"ALTER TABLE CATALOG_NAME.TABLE_NAME ALTER COLUMN COLUMN_NAME SET GENERATED BY DEFAULT AS IDENTITY (START WITH 2)");
    }
  }
}
