{
  for (  Database database : TestContext.getInstance().getAllDatabases()) {
    if (database instanceof DB2Database || database instanceof DB2Database) {
      super.testAddAutoIncrementStatement(database,null,null,"ALTER TABLE SCHEMA_NAME.TABLE_NAME ALTER COLUMN COLUMN_NAME SET GENERATED BY DEFAULT AS IDENTITY");
    }
  }
}
