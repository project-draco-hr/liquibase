{
  String canonicalCharsetName=Charset.forName(reader.getEncoding()).toString();
  assertEquals(expectedCharsetName,canonicalCharsetName);
  assertEquals(expectedDefault,reader.getDefaultEncoding());
}
