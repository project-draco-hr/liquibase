{
  return new Sql[]{new UnparsedSql("select definition from pg_views where viewname='" + statement.getViewName() + "' AND schemaname='"+ database.correctSchemaName(statement.getSchemaName())+ "'")};
}
