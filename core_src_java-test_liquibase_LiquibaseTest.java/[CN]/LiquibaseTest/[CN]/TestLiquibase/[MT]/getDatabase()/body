{
  if (database == null) {
    database=new OracleDatabase(){
      @Override public String getConnectionURL(){
        return url;
      }
      @Override public String getConnectionUsername(){
        return "testUser";
      }
    }
;
  }
  return database;
}
