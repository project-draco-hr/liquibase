{
  try {
    DatabaseChangeLog changeLog=new XMLChangeLogSAXParser().parse("liquibase/parser/xml/malformedChangeLog.xml",new HashMap<String,Object>(),new JUnitResourceAccessor());
  }
 catch (  Exception e) {
    assertTrue(e instanceof ChangeLogParseException);
    assertTrue(e.getMessage().startsWith("Error parsing line"));
  }
}
