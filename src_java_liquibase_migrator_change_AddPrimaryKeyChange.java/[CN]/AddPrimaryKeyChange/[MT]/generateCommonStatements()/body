{
  if (getConstraintName() == null) {
    return new String[]{"ALTER TABLE " + getTableName() + " ADD PRIMARY KEY ("+ getColumnNames()+ ")"};
  }
 else {
    return new String[]{"ALTER TABLE " + getTableName() + " ADD CONSTRAINT "+ getConstraintName()+ " PRIMARY KEY ("+ getColumnNames()+ ")"};
  }
}
