{
  int maxIndexLength;
  try {
    maxIndexLength=getConnection().getMetaData().getMaxIndexLength();
    return maxIndexLength == 0 || maxIndexLength >= 150 + 150 + 255 && super.canCreateChangeLogTable();
  }
 catch (  SQLException e) {
    throw new JDBCException(e);
  }
}
