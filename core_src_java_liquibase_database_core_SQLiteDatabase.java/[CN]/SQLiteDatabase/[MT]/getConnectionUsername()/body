{
  try {
    String username=getConnection().getMetaData().getUserName();
    if (username == null) {
      username="liquibase";
    }
    return username;
  }
 catch (  SQLException e) {
    throw new JDBCException(e);
  }
}
